{
  "name": "cs-analytics",
  "version": "1.0.0",
  "description": "PLG support digest site for StackOne CS",
  "scripts": {
    "build": "node scripts/fetch-all.js",
    "publish": "node publish.js --once",
    "watch": "node publish.js --watch"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "dotenv": "^16.6.1"
  }
}
